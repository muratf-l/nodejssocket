import {UserOnlineStatus} from '../Enums'

export default class GamePlayerInfo {
    public index: number;
    public name: string;
    public picture: string;
    public status: UserOnlineStatus;
}

